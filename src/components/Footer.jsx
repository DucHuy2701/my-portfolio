export default function Footer() {
  return (
    <footer className="text-center py-4 bg-light mt-5 border-top">
      <p className="mb-0 text-muted small">
        © {new Date().getFullYear()} Duc Huy Le. Built with ❤️ using React +
        Bootstrap.
      </p>
    </footer>
  );
}
